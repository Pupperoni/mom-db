# Mom DB - A Patient Database Application

## Installation

To install the app, run the following on the command line or terminal:

```bash
git clone https://github.com/Pupperoni/mom-db && cd mom-db
npm install # npm i
npm start
```